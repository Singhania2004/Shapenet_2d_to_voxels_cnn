# Single Image 3D Reconstruction

End-to-end system to reconstruct 3D objects from a single image using CNNs.

## Features
- CNN Encoder + 3D Decoder
- Voxel-based reconstruction
- Mesh export (.obj)
- Web visualization (planned)

## Setup
```bash
pip install -r requirements.txt
python train.py